# If not running interactively, don't do anything else
[[ $- != *i* ]] && return

# All the bash configs
source ~/.config/bash/rc

# Machine-local configuration, not managed by chezmoi
[[ -r "$HOME/.bashrc.local" ]] && source "$HOME/.bashrc.local"
