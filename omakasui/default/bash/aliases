# Navigation
md() {
  mkdir -p $1
  cd $1
}

# Tools
alias gl='glab'
b() {
  if command -v batcat &> /dev/null; then
    batcat "$@"
  else
    bat "$@"
  fi
}
alias lzg='lazygit'
alias lzd='lazydocker'